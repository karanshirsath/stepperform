import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { PolicyDetailsComponent } from '../app/components/policy-details/policy-details.component';
import { moduleMetadata } from "@storybook/angular";
import { date,select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { linkTo } from "@storybook/addon-links";

<Meta title="MDX/Policy Details Component" decorators={[
    moduleMetadata({
        declarations: [
            PolicyDetailsComponent
        ],
        provider: [{}]
    }),
]} parameters={{
    component: PolicyDetailsComponent
}}/>

# Policy Details Component 

This is the policy details component.

## MDX

Stotybook Docs allows you to mix stories and documentation in 
[MDX Format](https://mdxjs.com/)

MDX Combines Marksown and JSX.

## ComponentMeta SB5 Equivalent

This is roughly equivalent to SB5's `storiesOf` :

```
storiesOf('MDX|PolicyDetails Component',module)
    .addParameter({component: PolicyDetailsComponent})

```

## Stories

### Default

This is the default story. In this story the users can take a look at the overall
appearance of the login component.

<Preview>
  <Story name="Default">{{
    component: PolicyDetailsComponent,
    props:{
      
    },
    }}    
  </Story>
</Preview>

### Valid

This is the Valid story. In this story the users can take a look at the valid
appearance of the Policy Details component.

<Preview>
  <Story name="Valid">{{
    component: PolicyDetailsComponent,
    props:{
      policyDate: date('Policy Date',new Date()),
      policyLength: select('Policy Length',{'1 Month': 1, '3 Months': 3, '6 Months': 6, '12 Months': 12},3)
    },
    }}    
  </Story>
</Preview>

### Invalid

This is the Invalid story. In this story the users can take a look at the invalid
appearance of the policy details component.

<Preview>
  <Story name="Invalid">{{
    component: PolicyDetailsComponent,
    props:{
      policyDate: date('Policy Date',new Date()),
      policyLength: select('Policy Length',{'--SELECT--': 0,'1 Month': 1, '3 Months': 3, '6 Months': 6, '12 Months': 12},0)
    },
    }}    
  </Story>
</Preview>

### On Submission

This is the On Submission story. In this story the users can select any one of the given installment option by choosing radio button 
and for completion of transaction click on next button. A description is also given regarding the installment charge and savings a
user achieve after selecting the option.

<Preview>
  <Story name="On Submission">{{
    component: PolicyDetailsComponent,
    template: '<app-policy-details (OnSubmission)="success($event)"></app-policy-details>',
    props:{
        success: action('Success message')
    },
    }}    
  </Story>
</Preview>

### Linking of stories

This is the linking story. In this story the users can select any one of the given installment option by choosing radio button 
and for completion of transaction click on next button. On clicking the next button user will be redirected to next story.

<Preview>
  <Story name="Link to next story">{{
    component: PolicyDetailsComponent,
    template: '<app-policy-details (OnSubmission)="success($event)"></app-policy-details>',
    props:{
        success: linkTo('MDX/Installment Component','Default')
    },
    }}    
  </Story>
</Preview>
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { InsuringPartyComponent } from '../app/components/insuring-party/insuring-party.component';
import { moduleMetadata } from "@storybook/angular";
import { text,select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { linkTo } from "@storybook/addon-links";

<Meta title="MDX/Insuring Party Component" decorators={[
    moduleMetadata({
        declarations: [
            InsuringPartyComponent
        ],
        provider: [{}]
    }),
]} parameters={{
    component: InsuringPartyComponent
}}/>

# Insuring Party Component 

This is the delivery component.

## MDX

Stotybook Docs allows you to mix stories and documentation in 
[MDX Format](https://mdxjs.com/)

MDX Combines Marksown and JSX.

## ComponentMeta SB5 Equivalent

This is roughly equivalent to SB5's `storiesOf` :

```
storiesOf('MDX|Insuring Party Component',module)
    .addParameter({component: InsuringPartyComponent})

```

## Stories

### Default

This is the default story. In this story the users can take a look at the overall
appearance of the Insuring party component.

<Preview>
  <Story name="Default">{{
    component: InsuringPartyComponent,
    props:{
      
    },
    }}    
  </Story>
</Preview>

### Wrong input for EGN/EIK/LNCH Number

<Preview>
  <Story name="Wrong EGN/EIK/LNCH">{{
    component: InsuringPartyComponent,
    props:{
      egn: text('EGN/EIK/LNCH','123456'),
      address: select('Address',{'--SELECT--': '','Sofia': 'Sofia', 'Varna': 'Varna','Burgas': 'Burgas'},'Sofia'),
      email: text('Email','amit@gmail.com')
    },
    }}    
  </Story>
</Preview>

### Wrong input for email

<Preview>
  <Story name="Wrong Email">{{
    component: InsuringPartyComponent,
    props:{
      egn: text('EGN/EIK/LNCH','1234567890'),
      address: select('Address',{'--SELECT--': '','Sofia': 'Sofia', 'Varna': 'Varna','Burgas': 'Burgas'},'Sofia'),
      email: text('Email','xyz@g')
    },
    }}    
  </Story>
</Preview>

### Valid Story

<Preview>
  <Story name="All fields are correct">{{
    component: InsuringPartyComponent,
    props:{
      egn: text('EGN/EIK/LNCH','1234567890'),
      address: select('Address',{'--SELECT--': '','Sofia': 'Sofia', 'Varna': 'Varna','Burgas': 'Burgas'},'Sofia'),
      email: text('Email','xyz@gmail.com')
    },
    }}    
  </Story>
</Preview>

### On Submission

This is the On Submission story. In this story the users can select any one of the given installment option by choosing radio button 
and for completion of transaction click on next button. A description is also given regarding the installment charge and savings a
user achieve after selecting the option.

<Preview>
  <Story name="On Submission">{{
    component: InsuringPartyComponent,
    template: '<app-insuring-party (OnSubmission)="success($event)"></app-insuring-party>',
    props:{
        success: action('Success message')
    },
    }}    
  </Story>
</Preview>

### Linking of stories

This is the linking story. In this story the users can select any one of the given installment option by choosing radio button 
and for completion of transaction click on next button. On clicking the next button user will be redirected to next story.

<Preview>
  <Story name="Link to next story">{{
    component: InsuringPartyComponent,
    template: '<app-insuring-party (OnSubmission)="success($event)"></app-insuring-party>',
    props:{
        success: linkTo('MDX/Policy Details Component','Default')
    },
    }}    
  </Story>
</Preview>